# Training Folder

This folder can store simple training notes or experiment notes.

The main training code is in `train.py` to keep the project easy to follow.

YOLO training results are saved in `outputs/training_results/`.
